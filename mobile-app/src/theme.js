// Adana Sınav Kulübü — mavi-merkezli marka paleti (web ile aynı)
export const colors = {
  primary: '#003b78',     // lacivert
  blue: '#2563eb',        // birincil mavi
  brightBlue: '#3b82f6',  // canlı mavi / vurgu
  accent: '#06b6d4',      // cyan (bilgi)
  success: '#10b981',     // sonuç / başarı
  warning: '#f59e0b',     // uyarı / bekleyen
  danger: '#ef4444',      // sil / hata
  text: '#1e293b',
  textMuted: '#64748b',
  border: '#e2e8f0',
  bg: '#f8fafc',
  card: '#ffffff',
  white: '#ffffff',
};

// Tipografi ölçeği (web ile aynı)
export const fs = {
  xs: 12, sm: 14, base: 16, md: 18, lg: 20, xl: 24, xxl: 32,
};

export const radius = { card: 16, button: 12, pill: 20 };
