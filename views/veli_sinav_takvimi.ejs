<%- include('partials/header-dashboard', { title: 'Sınav Takvimi - Sınav Merkezi', user: user }) %>
<%- include('partials/veli-panel-styles') %>

<style>
    /* Mor tema header */
    .vp-calendar-header {
        background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
        color: white;
        padding: clamp(16px, 4vw, 24px) 0;
        margin: -20px -12px clamp(16px, 4vw, 24px);
        border-radius: 0 0 var(--vp-radius-lg) var(--vp-radius-lg);
    }

    .vp-calendar-header h2 {
        font-size: clamp(1.1rem, 4vw, 1.4rem);
        font-weight: 700;
        margin-bottom: 4px;
    }

    .vp-calendar-header p {
        font-size: clamp(0.8rem, 3vw, 0.9rem);
        opacity: 0.9;
        margin: 0;
    }

    /* Ogrenci Bilgi Karti */
    .vp-student-info-card {
        background: white;
        border-radius: var(--vp-radius);
        padding: clamp(12px, 3vw, 16px);
        margin-bottom: 16px;
        border: 1px solid var(--vp-border);
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .vp-student-info-avatar {
        width: clamp(44px, 10vw, 52px);
        height: clamp(44px, 10vw, 52px);
        background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
        border-radius: var(--vp-radius);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: clamp(1.1rem, 4vw, 1.4rem);
        font-weight: 700;
        color: white;
        flex-shrink: 0;
    }

    .vp-student-info-name {
        font-weight: 700;
        color: var(--vp-text);
        font-size: clamp(0.95rem, 3.5vw, 1.05rem);
    }

    .vp-student-info-meta {
        font-size: clamp(0.75rem, 2.8vw, 0.85rem);
        color: var(--vp-text-muted);
    }

    /* Sinav Kartlari */
    .vp-exam-card {
        background: white;
        border-radius: var(--vp-radius);
        padding: clamp(12px, 3vw, 16px);
        margin-bottom: 12px;
        border: 1px solid var(--vp-border);
    }

    .vp-exam-date-box {
        background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
        color: white;
        border-radius: var(--vp-radius);
        padding: clamp(10px, 2.5vw, 14px);
        text-align: center;
        min-width: clamp(56px, 14vw, 70px);
        flex-shrink: 0;
    }

    .vp-exam-date-box .day {
        font-size: clamp(1.4rem, 5vw, 1.8rem);
        font-weight: 800;
        line-height: 1;
    }

    .vp-exam-date-box .month {
        font-size: clamp(0.7rem, 2.5vw, 0.8rem);
        opacity: 0.9;
        text-transform: uppercase;
    }

    .vp-exam-title {
        font-weight: 700;
        color: var(--vp-text);
        font-size: clamp(0.9rem, 3.2vw, 1rem);
        margin-bottom: 4px;
    }

    .vp-exam-details {
        font-size: clamp(0.75rem, 2.8vw, 0.85rem);
        color: var(--vp-text-muted);
    }

    /* Mor Header icin */
    .vp-purple-section {
        background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
        color: white;
        padding: clamp(12px, 3vw, 16px);
        border-radius: var(--vp-radius) var(--vp-radius) 0 0;
        margin: -1px -1px 0 -1px;
    }

    .vp-purple-section h5 {
        font-weight: 700;
        margin: 0;
        font-size: clamp(0.9rem, 3.2vw, 1rem);
    }
</style>

<!-- Page Header -->
<div class="vp-calendar-header">
    <div class="container">
        <h2><i class="bi bi-calendar-event"></i> Sinav Takvimi</h2>
        <p><%= ogrenci.ad_soyad %> - Yaklasan sinavlar</p>
    </div>
</div>

<div class="container">
    <% if (error) { %>
    <div class="alert alert-danger alert-dismissible fade show" role="alert" style="border-radius: var(--vp-radius); font-size: 0.9rem;">
        <i class="bi bi-exclamation-triangle"></i> <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    <% } %>

    <% if (success) { %>
    <div class="alert alert-success alert-dismissible fade show" role="alert" style="border-radius: var(--vp-radius); font-size: 0.9rem;">
        <i class="bi bi-check-circle"></i> <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    <% } %>

    <!-- Ogrenci Bilgisi -->
    <div class="vp-student-info-card">
        <div class="vp-student-info-avatar">
            <%= ogrenci.ad_soyad.charAt(0).toUpperCase() %>
        </div>
        <div>
            <div class="vp-student-info-name"><%= ogrenci.ad_soyad %></div>
            <div class="vp-student-info-meta">
                <i class="bi bi-building"></i> <%= ogrenci.okul || 'Belirtilmemis' %> |
                <i class="bi bi-book"></i> <%= ogrenci.sinif || '-' %>
            </div>
        </div>
    </div>

    <!-- Sinav Listesi -->
    <div class="vp-card" style="padding: 0; overflow: hidden;">
        <div class="vp-purple-section">
            <h5><i class="bi bi-calendar-check"></i> Yaklasan Sinavlar</h5>
        </div>

        <div style="padding: clamp(12px, 3vw, 16px);">
            <% if (takvim && takvim.length > 0) { %>
                <% takvim.forEach(function(sinav) {
                    const tarih = new Date(sinav.tarih);
                    const gun = tarih.getDate();
                    const ay = tarih.toLocaleDateString('tr-TR', { month: 'short' });
                    const tamTarih = tarih.toLocaleDateString('tr-TR');
                    const bugun = new Date();
                    const gunFarki = Math.ceil((tarih - bugun) / (1000 * 60 * 60 * 24));

                    let durumText = '';
                    let durumClass = '';
                    if (gunFarki < 0) {
                        durumText = 'Gecti';
                        durumClass = 'vp-badge-secondary';
                    } else if (gunFarki === 0) {
                        durumText = 'Bugun!';
                        durumClass = 'vp-badge-danger';
                    } else if (gunFarki <= 7) {
                        durumText = gunFarki + ' gun';
                        durumClass = 'vp-badge-warning';
                    } else {
                        durumText = gunFarki + ' gun';
                        durumClass = 'vp-badge-success';
                    }
                %>
                <div class="vp-exam-card">
                    <div class="d-flex align-items-center gap-3">
                        <div class="vp-exam-date-box">
                            <div class="day"><%= gun %></div>
                            <div class="month"><%= ay %></div>
                        </div>

                        <div class="flex-grow-1" style="min-width: 0;">
                            <div class="vp-exam-title"><%= sinav.sinav_adi || 'Sinav' %></div>
                            <div class="vp-exam-details">
                                <i class="bi bi-calendar3"></i> <%= tamTarih %>
                                <% if (sinav.saat) { %>
                                    | <i class="bi bi-clock"></i> <%= sinav.saat %>
                                <% } %>
                            </div>
                            <% if (sinav.yer) { %>
                            <div class="vp-exam-details">
                                <i class="bi bi-geo-alt"></i> <%= sinav.yer %>
                            </div>
                            <% } %>
                        </div>

                        <span class="vp-badge <%= durumClass %>"><%= durumText %></span>
                    </div>

                    <% if (sinav.aciklama) { %>
                    <div class="vp-exam-details" style="margin-top: 8px; padding-top: 8px; border-top: 1px solid var(--vp-border);">
                        <i class="bi bi-info-circle"></i> <%= sinav.aciklama %>
                    </div>
                    <% } %>
                </div>
                <% }); %>
            <% } else { %>
                <div class="vp-empty" style="margin: 0;">
                    <i class="bi bi-calendar-x"></i>
                    <h4>Henuz Sinav Yok</h4>
                    <p>Bu ogrenci icin henuz planlanmis bir sinav bulunmamaktadir.</p>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Bilgi -->
    <div class="vp-info-box" style="background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%); border-color: #93c5fd; margin-top: 16px;">
        <h5 style="color: #1e40af;"><i class="bi bi-info-circle-fill"></i> Bilgi</h5>
        <p style="color: #1e40af; margin: 0; font-size: clamp(0.8rem, 3vw, 0.9rem);">
            Sinav takvimi rehber ogretmeniniz tarafindan olusturulur ve guncellenir.
        </p>
    </div>

    <!-- Geri Butonu -->
    <div class="mt-4">
        <a href="/veli/dashboard" class="vp-btn vp-btn-secondary">
            <i class="bi bi-arrow-left"></i> Geri
        </a>
    </div>
</div>

<%- include('partials/footer-dashboard') %>
